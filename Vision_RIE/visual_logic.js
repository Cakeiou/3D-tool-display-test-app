/* eslint-disable */

/**
 * Generated by Verge3D Puzzles v.4.10.0
 * Thu, 06 Nov 2025 21:02:06 GMT
 * Prefer not editing this file as your changes may get overridden once Puzzles are saved.
 * Check out https://www.soft8soft.com/docs/manual/en/introduction/Using-JavaScript.html
 * for the information on how to add your own JavaScript to Verge3D apps.
 */
function createPL(v3d = window.v3d) {

// global variables used in the init tab
const _initGlob = {
    percentage: 0,
    output: {
        initOptions: {
            fadeAnnotations: true,
            useBkgTransp: false,
            preserveDrawBuf: false,
            useCompAssets: false,
            useFullscreen: true,
            useCustomPreloader: false,
            preloaderStartCb: function() {},
            preloaderProgressCb: function() {},
            preloaderEndCb: function() {},
        },
    },
};


// global variables/constants used by puzzles' functions
var _pGlob = {};

_pGlob.objCache = new Map();
_pGlob.fadeAnnotations = true;
_pGlob.pickedObject = '';
_pGlob.hoveredObject = '';
_pGlob.mediaElements = {};
_pGlob.loadedFile = '';
_pGlob.states = [];
_pGlob.percentage = 0;
_pGlob.openedFile = '';
_pGlob.openedFileMeta = {};
_pGlob.xrSessionAcquired = false;
_pGlob.xrSessionCallbacks = [];
_pGlob.screenCoords = new v3d.Vector2();
_pGlob.intervalTimers = {};
_pGlob.customEvents = new v3d.EventDispatcher();
_pGlob.eventListeners = [];
_pGlob.htmlElements = new Set();
_pGlob.materialsCache = new Map();

_pGlob.AXIS_X = new v3d.Vector3(1, 0, 0);
_pGlob.AXIS_Y = new v3d.Vector3(0, 1, 0);
_pGlob.AXIS_Z = new v3d.Vector3(0, 0, 1);
_pGlob.MIN_DRAG_SCALE = 10e-4;
_pGlob.SET_OBJ_ROT_EPS = 1e-8;

_pGlob.vec2Tmp = new v3d.Vector2();
_pGlob.vec2Tmp2 = new v3d.Vector2();
_pGlob.vec3Tmp = new v3d.Vector3();
_pGlob.vec3Tmp2 = new v3d.Vector3();
_pGlob.vec3Tmp3 = new v3d.Vector3();
_pGlob.vec3Tmp4 = new v3d.Vector3();
_pGlob.eulerTmp = new v3d.Euler();
_pGlob.eulerTmp2 = new v3d.Euler();
_pGlob.quatTmp = new v3d.Quaternion();
_pGlob.quatTmp2 = new v3d.Quaternion();
_pGlob.colorTmp = new v3d.Color();
_pGlob.mat4Tmp = new v3d.Matrix4();
_pGlob.planeTmp = new v3d.Plane();
_pGlob.raycasterTmp = new v3d.Raycaster(); // always check visibility

const createPzLib = ({ v3d=null, appInstance=null }) => {
    function generateUniqueName(name, isUniqueCb) {
        let uniqueName = name;
    
        if (isUniqueCb !== undefined) {
            while (!isUniqueCb(uniqueName)) {
                const r = uniqueName.match(/^(.*?)(\d+)$/);
                if (!r) {
                    uniqueName += '2';
                } else {
                    uniqueName = r[1] + (Number.parseInt(r[2], 10) + 1);
                }
            }
        }
    
        return uniqueName;
    }
        
    function isObjectWorthProcessing(obj) {
        return obj.name !== '' &&
                !(obj.isMesh && obj.isMaterialGeneratedMesh) &&
                !obj.isAuxClippingMesh;
    }
        
    function getObjectByName(objName) {
        let objFound = null;
    
        // COMPAT: <4.9.0, old engine, new puzzles
        const isID = v3d.MathUtils.checkUUID ? v3d.MathUtils.checkUUID(objName) : false;
    
        const pGlobAvailable = _pGlob !== undefined;
        if (pGlobAvailable)
            objFound = _pGlob.objCache.get(objName);
    
        if (objFound && (isID ? objFound.uuid === objName : objFound.name === objName))
            return objFound;
    
        function findValidByName(obj, objName) {
            if ((isID ? obj.uuid === objName : obj.name === objName) && isObjectWorthProcessing(obj))
                return obj;
    
            for (let i = 0; i < obj.children.length; i++) {
                const child = obj.children[i];
                const object = findValidByName(child, objName);
                if (object !== null)
                    return object;
            }
    
            return null;
        }
    
        if (appInstance.scene) {
            objFound = findValidByName(appInstance.scene, objName);
            if (objFound && pGlobAvailable)
                _pGlob.objCache.set(objName, objFound);
        }
    
        return objFound;
    }
        
    function generateUniqueObjectName(name) {
        return generateUniqueName(name, name => !Boolean(getObjectByName(name)));
    }
        
    function getObjectNamesByGroupName(groupName) {
        const objNameList = [];
        appInstance.scene.traverse(obj => {
            if (isObjectWorthProcessing(obj)) {
                const objGroupNames = obj.groupNames;
                if (!objGroupNames) {
                    return;
                }
    
                for (let i = 0; i < objGroupNames.length; i++) {
                    const objGroupName = objGroupNames[i];
                    if (objGroupName === groupName) {
                        objNameList.push(obj.name);
                    }
                }
            }
        });
        return objNameList;
    }
        
    function getAllObjectNames() {
        const objNameList = [];
        appInstance.scene.traverse(obj => {
            if (isObjectWorthProcessing(obj)) {
                objNameList.push(obj.name);
            }
        });
        return objNameList;
    }
        
    function retrieveObjectNamesAccum(currObjNames, namesAccum) {
        if (typeof currObjNames === 'string') {
            namesAccum.push(currObjNames);
        } else if (Array.isArray(currObjNames) && currObjNames[0] === 'GROUP') {
            const newObjNames = getObjectNamesByGroupName(currObjNames[1]);
            for (let i = 0; i < newObjNames.length; i++) {
                namesAccum.push(newObjNames[i]);
            }
        } else if (Array.isArray(currObjNames) && currObjNames[0] === 'ALL_OBJECTS') {
            const newObjNames = getAllObjectNames();
            for (let i = 0; i < newObjNames.length; i++) {
                namesAccum.push(newObjNames[i]);
            }
        } else if (Array.isArray(currObjNames)) {
            for (let i = 0; i < currObjNames.length; i++) {
                retrieveObjectNamesAccum(currObjNames[i], namesAccum);
            }
        }
    }
        
    function retrieveObjectNames(objNames) {
        const accum = [];
        retrieveObjectNamesAccum(objNames, accum);
        return accum.filter(name => name !== '');
    }
        
    function areListenersSame(target0, type0, listener0, optionsOrUseCapture0,
            target1, type1, listener1, optionsOrUseCapture1) {
        const capture0 = Boolean(optionsOrUseCapture0 instanceof Object
                ? optionsOrUseCapture0.capture : optionsOrUseCapture0);
        const capture1 = Boolean(optionsOrUseCapture1 instanceof Object
                ? optionsOrUseCapture1.capture : optionsOrUseCapture1);
        return target0 === target1 && type0 === type1 && listener0 === listener1
                && capture0 === capture1;
    }
        
    function bindListener(target, type, listener, optionsOrUseCapture) {
        const alreadyExists = _pGlob.eventListeners.some(elem => {
            return areListenersSame(elem.target, elem.type, elem.listener,
                    elem.optionsOrUseCapture, target, type, listener,
                    optionsOrUseCapture);
        });
    
        if (!alreadyExists) {
            target.addEventListener(type, listener, optionsOrUseCapture);
            _pGlob.eventListeners.push({ target, type, listener,
                    optionsOrUseCapture });
        }
    }
        
    function getElement(id, isParent=false) {
        let elem;
        if (Array.isArray(id) && id[0] === 'CONTAINER') {
            if (appInstance !== null) {
                elem = appInstance.container;
            } else if (typeof _initGlob !== 'undefined') {
                // if we are on the initialization stage, we still can have access
                // to the container element
                const contId = _initGlob.container;
                elem = isParent ? parent.document.getElementById(contId)
                        : document.getElementById(contId);
            }
        } else if (Array.isArray(id) && id[0] === 'WINDOW') {
            elem = isParent ? parent : window;
        } else if (Array.isArray(id) && id[0] === 'DOCUMENT') {
            elem = isParent ? parent.document : document;
        } else if (Array.isArray(id) && id[0] === 'BODY') {
            elem = isParent ? parent.document.body : document.body;
        } else if (Array.isArray(id) && id[0] === 'QUERYSELECTOR') {
            elem = isParent ? parent.document.querySelector(id)
                    : document.querySelector(id);
        } else {
            elem = isParent ? parent.document.getElementById(id)
                    : document.getElementById(id);
        }
        return elem;
    }
        
    function getElements(ids, isParent=false) {
        const elems = [];
        if (Array.isArray(ids) && ids[0] !== 'CONTAINER' && ids[0] !== 'WINDOW'
                && ids[0] !== 'DOCUMENT' && ids[0] !== 'BODY'
                && ids[0] !== 'QUERYSELECTOR') {
            for (let i = 0; i < ids.length; i++) {
                elems.push(getElement(ids[i], isParent));
            }
        } else {
            elems.push(getElement(ids, isParent));
        }
        return elems;
    }
        
    function getSceneAnimFrameRate(scene) {
        if (scene && 'animFrameRate' in scene.userData) {
            return scene.userData.animFrameRate;
        }
        return 24;
    }
        
    function getSceneByAction(action) {
        const root = action.getRoot();
        let scene = root.type === 'Scene' ? root : null;
        root.traverseAncestors(ancObj => {
            if (ancObj.type === 'Scene') {
                scene = ancObj;
            }
        });
        return scene;
    }
        
    function transformCoordsSpace(coords, spaceFrom, spaceTo, noSignChange=false) {
    
        if (spaceFrom === spaceTo) {
            return coords;
        }
    
        const y = coords.y;
        const z = coords.z;
    
        if (spaceFrom === 'Z_UP_RIGHT' && spaceTo === 'Y_UP_RIGHT') {
            coords.y = z;
            coords.z = noSignChange ? y : -y;
        } else if (spaceFrom === 'Y_UP_RIGHT' && spaceTo === 'Z_UP_RIGHT') {
            coords.y = noSignChange ? z : -z;
            coords.z = y;
        } else {
            console.error('transformCoordsSpace: Unsupported coordinate space');
        }
    
        return coords;
    }
        
    function getSceneCoordSystem() {
        const scene = appInstance.scene;
        if (scene && 'coordSystem' in scene.userData) {
            return scene.userData.coordSystem;
        }
    
        return 'Y_UP_RIGHT';
    }

    return {
        generateUniqueObjectName, getObjectByName, retrieveObjectNames, bindListener,
        getElements, getSceneAnimFrameRate, getSceneByAction, transformCoordsSpace,
        getSceneCoordSystem,
    };
};

var PL = {};



// backward compatibility
if (v3d[Symbol.toStringTag] !== 'Module') {
    v3d.PL = v3d.puzzles = PL;
}

PL.procedures = PL.procedures || {};




PL.execInitPuzzles = function(options) {
    // always null, should not be available in "init" puzzles
    var appInstance = null;
    // app is more conventional than appInstance (used in exec script and app templates)
    var app = null;

    const PzLib = createPzLib({ v3d });

    // provide the container's id to puzzles that need access to the container
    _initGlob.container = options !== undefined && 'container' in options
            ? options.container : "";

    

    // initSettings puzzle
_initGlob.output.initOptions.fadeAnnotations = true;
_initGlob.output.initOptions.useBkgTransp = true;
_initGlob.output.initOptions.preserveDrawBuf = false;
_initGlob.output.initOptions.useCompAssets = false;
_initGlob.output.initOptions.useFullscreen = true;

    return _initGlob.output;
}

PL.init = function(appInstance, initOptions) {

// app is more conventional than appInstance (used in exec script and app templates)
var app = appInstance;

const PzLib = createPzLib({ v3d, appInstance });

initOptions = initOptions || {};

if ('fadeAnnotations' in initOptions) {
    _pGlob.fadeAnnotations = initOptions.fadeAnnotations;
}

this.procedures["UnAnim3"] = UnAnim3;
this.procedures["UnAnim2"] = UnAnim2;
this.procedures["UnAnim1"] = UnAnim1;

var frontCabOpen, backCabOpen, vacChambOpen;

// setCSSRuleStyle puzzle
function setCSSRuleStyle(prop, value, id, isParent, mediaRule) {
    const styles = (isParent) ? parent.document.styleSheets : document.styleSheets;
    for (let i = 0; i < styles.length; i++) {
        /**
         * workaround for "DOMException: Failed to read the 'cssRules' property
         * from 'CSSStyleSheet': Cannot access rules"
         */
        let cssRules;
        try {
            cssRules = styles[i].cssRules;
        }
        catch (e) { continue; }

        for (let j = 0; j < cssRules.length; j++) {
            const cssRule = cssRules[j];
            if (!mediaRule && cssRule.selectorText == id)
                cssRule.style[prop] = value;
            else if (mediaRule && cssRule.media && cssRule.media.mediaText == mediaRule) {
                const cssRulesMedia = cssRule.cssRules;
                for (let k = 0; k < cssRulesMedia.length; k++) {
                    if (cssRulesMedia[k].selectorText == id)
                        cssRulesMedia[k].style[prop] = value;
                }
            }
        }
    }
}

// addAnnotation and removeAnnotation puzzles
function handleAnnot(add, annot, objSelector, contents, id, name) {
    var objNames = PzLib.retrieveObjectNames(objSelector);

    for (var i = 0; i < objNames.length; i++) {
        var objName = objNames[i];
        if (!objName)
            continue;
        var obj = PzLib.getObjectByName(objName);
        if (!obj)
            continue;
        // check if it already has an annotation and remove it
        for (var j = 0; j < obj.children.length; j++) {
            var child = obj.children[j];
            if (child.type == "Annotation") {
                // delete all childs of annotation
                child.traverse(function(child2) {
                    if (child2.isAnnotation)
                        child2.dispose();
                    });
                obj.remove(child);
            }
        }
        if (add) {
            var aObj = new v3d.Annotation(appInstance.container, annot, contents);
            aObj.name = PzLib.generateUniqueObjectName(name ? name : annot);
            aObj.fadeObscured = _pGlob.fadeAnnotations;
            if (id) {
                aObj.annotation.id = id;
                aObj.annotationDialog.id = id+'_dialog';
            }
            obj.add(aObj);
        }
    }
}

// callJSFunction puzzle
function getJSFunction(funcName) {
    var jsFunc = appInstance.ExternalInterface[funcName];
    if (typeof jsFunc == "function")
        return jsFunc;
    else
        return function() {};
}

// eventHTMLElem puzzle
function eventHTMLElem(eventType, ids, isParent, callback) {
    var elems = PzLib.getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem)
            continue;

        PzLib.bindListener(elem, eventType, callback);
    }
}

// setHTMLElemStyle puzzle
function setHTMLElemStyle(prop, value, ids, isParent) {
    var elems = PzLib.getElements(ids, isParent);
    for (var i = 0; i < elems.length; i++) {
        var elem = elems[i];
        if (!elem || !elem.style)
            continue;
        elem.style[prop] = value;
    }
}

// setActiveCamera puzzle
function setActiveCamera(camName) {
    var camera = PzLib.getObjectByName(camName);
    if (!camera || !camera.isCamera || appInstance.getCamera() == camera)
        return;
    appInstance.setCamera(camera);
}

_pGlob.animMixerCallbacks = [];

const initAnimationMixer = function() {

    function onMixerFinished(e) {
        const cb = _pGlob.animMixerCallbacks;
        const found = [];
        for (let i = 0; i < cb.length; i++) {
            if (cb[i][0] == e.action) {
                cb[i][0] = null; // desactivate
                found.push(cb[i][1]);
            }
        }
        for (let i = 0; i < found.length; i++) {
            found[i]();
        }
    }

    return function initAnimationMixer() {
        if (appInstance.mixer && !appInstance.mixer.hasEventListener('finished', onMixerFinished)) {
            PzLib.bindListener(appInstance.mixer, 'finished', onMixerFinished);
        }
    };

}();

// animation puzzles
function operateAnimation(operation, animations, from, to, loop, speed, callback, rev) {
    if (!animations)
        return;
    // input can be either single obj or array of objects
    if (typeof animations == "string")
        animations = [animations];

    function processAnimation(animName) {
        const action = v3d.SceneUtils.getAnimationActionByName(appInstance, animName);
        if (!action)
            return;

        let scene, frameRate, timeScale, callbacks;

        switch (operation) {
        case 'PLAY':
            if (!action.isRunning()) {
                action.reset();

                if (loop == 'AUTO') {
                    // COMPAT: < 4.10, old engine, new puzzles (no autoParams)
                    if (action.autoParams) {
                        action.loop = action.autoParams.loop;
                        action.repetitions = action.autoParams.repetitions;
                    } else {
                        action.repetitions = Infinity;
                    }
                } else {
                    action.loop = v3d[loop];
                    action.repetitions = Infinity;
                }

                scene = PzLib.getSceneByAction(action);
                frameRate = PzLib.getSceneAnimFrameRate(scene);
                timeScale = Math.abs(parseFloat(speed));

                if (rev)
                    timeScale *= -1;

                action.timeScale = timeScale;
                action.timeStart = from !== null ? from/frameRate : 0;
                if (to !== null) {
                    action.getClip().duration = to/frameRate;
                } else {
                    action.getClip().resetDuration();
                }
                action.time = timeScale >= 0 ? action.timeStart : action.getClip().duration;

                action.paused = false;
                action.play();

                // push unique callbacks only
                callbacks = _pGlob.animMixerCallbacks;
                let found = false;
                for (let j = 0; j < callbacks.length; j++)
                    if (callbacks[j][0] == action && callbacks[j][1] == callback)
                        found = true;

                if (!found)
                    _pGlob.animMixerCallbacks.push([action, callback]);
            }
            break;
        case 'STOP':
            action.stop();

            // remove callbacks
            callbacks = _pGlob.animMixerCallbacks;
            for (let j = 0; j < callbacks.length; j++)
                if (callbacks[j][0] == action) {
                    callbacks.splice(j, 1);
                    j--;
                }

            break;
        case 'PAUSE':
            action.paused = true;
            break;
        case 'RESUME':
            action.paused = false;
            break;
        case 'SET_FRAME':
            scene = PzLib.getSceneByAction(action);
            frameRate = PzLib.getSceneAnimFrameRate(scene);
            action.time = from ? from/frameRate : 0;
            action.play();
            action.paused = true;
            break;
        case 'SET_SPEED':
            timeScale = parseFloat(speed);
            action.timeScale = rev ? -timeScale : timeScale;
            break;
        }
    }

    for (let i = 0; i < animations.length; i++) {
        const animName = animations[i];
        if (animName)
            processAnimation(animName);
    }

    initAnimationMixer();
}

// applyObjLocalTransform puzzle
function applyObjLocalTransform(objSelector, mode, vector) {

    var objNames = PzLib.retrieveObjectNames(objSelector);
    var x = vector[0] || 0;
      var y = vector[1] || 0;
      var z = vector[2] || 0;

    var defValue = mode == "scale" ? 1 : 0;
    if (typeof x != "number") x = defValue;
    if (typeof y != "number") y = defValue;
    if (typeof z != "number") z = defValue;

    var coords = PzLib.transformCoordsSpace(_pGlob.vec3Tmp.set(x, y, z),
            PzLib.getSceneCoordSystem(), 'Y_UP_RIGHT', mode == 'scale');

    for (var i = 0; i < objNames.length; i++) {
        var objName = objNames[i];
        if (!objName) continue;

        var obj = PzLib.getObjectByName(objName);
        if (!obj) continue;

        // don't transform values for cameras, their local space happens
        // to be the same as for Blender/Max cameras, bcz their different
        // rest orientation balances difference in coordinate systems
        var useTransformed = !obj.isCamera;
        var xVal = useTransformed ? coords.x : x;
        var yVal = useTransformed ? coords.y : y;
        var zVal = useTransformed ? coords.z : z;

        switch (mode) {
        case "position":
            if (_pGlob.xrSessionAcquired && obj.isCamera) {
                v3d.WebXRUtils.translateVRCamera(obj, _pGlob.AXIS_X, xVal);
                v3d.WebXRUtils.translateVRCamera(obj, _pGlob.AXIS_Y, yVal);
                v3d.WebXRUtils.translateVRCamera(obj, _pGlob.AXIS_Z, zVal);
            } else {
                obj.translateX(xVal);
                obj.translateY(yVal);
                obj.translateZ(zVal);
            }
            break;
        case "rotation":
            if (_pGlob.xrSessionAcquired && obj.isCamera) {
                v3d.WebXRUtils.rotateVRCamera(obj, _pGlob.AXIS_X, v3d.MathUtils.degToRad(xVal));
                v3d.WebXRUtils.rotateVRCamera(obj, _pGlob.AXIS_Y, v3d.MathUtils.degToRad(yVal));
                v3d.WebXRUtils.rotateVRCamera(obj, _pGlob.AXIS_Z, v3d.MathUtils.degToRad(zVal));
            } else {
                obj.rotateX(v3d.MathUtils.degToRad(xVal));
                obj.rotateY(v3d.MathUtils.degToRad(yVal));
                obj.rotateZ(v3d.MathUtils.degToRad(zVal));
            }
            break;
        case "scale":
            obj.scale.x *= xVal;
            obj.scale.y *= yVal;
            obj.scale.z *= zVal;
            break;
        }

        obj.updateWorldMatrix(false, true);
    }
}

// Describe this function...
function UnAnim3() {
    vacChambOpen = false;
    setActiveCamera('Camera');
    operateAnimation('STOP', 'Vacuum_Chamber', null, null, 'AUTO', 1, function() {}, false);
    operateAnimation('PLAY', 'Vacuum_Chamber', 1, 60, 'LoopOnce', 1, function() {}, true);
    applyObjLocalTransform('Open3', 'position', [-100, 0, 0]);
    setCSSRuleStyle('display', 'none', '.top-left-btn', true, '');
    setHTMLElemStyle('display', 'none', 'Anno_Vac', false);
}

// Describe this function...
function UnAnim2() {
    backCabOpen = false;
    setActiveCamera('Camera');
    operateAnimation('STOP', 'Door_Back', null, null, 'AUTO', 1, function() {}, false);
    operateAnimation('PLAY', 'Door_Back', 1, 60, 'LoopOnce', 1, function() {}, true);
    applyObjLocalTransform('Open2', 'position', [-100, 0, 0]);
    setCSSRuleStyle('display', 'none', '.top-left-btn', true, '');
    setHTMLElemStyle('display', 'none', 'Anno_GasCabinet', false);
    setHTMLElemStyle('display', 'none', 'Anno_MFC', false);
    setHTMLElemStyle('display', 'none', 'Anno_PB_Valve', false);
}

// Describe this function...
function UnAnim1() {
    frontCabOpen = false;
    setActiveCamera('Camera');
    operateAnimation('STOP', 'Door_FrontL', null, null, 'AUTO', 1, function() {}, false);
    operateAnimation('STOP', 'Door_FrontR', null, null, 'AUTO', 1, function() {}, false);
    operateAnimation('PLAY', 'Door_FrontL', 1, 60, 'LoopOnce', 1, function() {}, true);
    operateAnimation('PLAY', 'Door_FrontR', 1, 60, 'LoopOnce', 1, function() {}, true);
    applyObjLocalTransform('Open', 'position', [-100, 0, 0]);
    setHTMLElemStyle('display', 'none', 'Anno_Throttle', false);
    setHTMLElemStyle('display', 'none', 'Anno_Turbo', false);
    setHTMLElemStyle('display', 'none', 'Anno_WRG', false);
    setHTMLElemStyle('display', 'none', 'Anno_Capman', false);
    setHTMLElemStyle('display', 'none', 'Anno_DV_Valve', false);
}


setCSSRuleStyle('display', 'none', '.v3d-annotation-transparent', false, '');
handleAnnot(true, 'Open', 'Anim_FrontCab', '', 'Anim_FrontCab', undefined);
handleAnnot(true, 'Open', 'Anim_BackCab', '', 'Anim_BackCab', undefined);
handleAnnot(true, 'Open', 'Anim_Vac', '', 'Anim_Vac', undefined);
frontCabOpen = false;
backCabOpen = false;
vacChambOpen = false;
handleAnnot(true, 'i', 'Anno_Computer', 'control unit for programming, monitoring, and running the etching processes', 'Anno_Computer', undefined);
handleAnnot(true, 'i', 'Anno_StatusLT', ' <img width="" height="" src="StackLight.png" alt="stack light"> ', 'Anno_StatusLT', undefined);
handleAnnot(true, 'i', 'Anno_DryScroll', '', 'Anno_DryScroll', undefined);

eventHTMLElem('click', 'Anno_DryScroll', false, function(event) {
    getJSFunction('changeIframe')('https://en.wikipedia.org/wiki/Roughing_pump');
});

handleAnnot(true, 'i', 'Anno_Throttle', '', 'Anno_Throttle', undefined);
handleAnnot(true, 'i', 'Anno_Turbo', '', 'Anno_Turbo', undefined);
handleAnnot(true, 'i', 'Anno_WRG', '', 'Anno_WRG', undefined);
handleAnnot(true, 'i', 'Anno_Capman', '', 'Anno_Capman', undefined);
handleAnnot(true, 'i', 'Anno_DV_Valve', '', 'Anno_DV_Valve', undefined);
setHTMLElemStyle('display', 'none', 'Anno_Throttle', false);
setHTMLElemStyle('display', 'none', 'Anno_Turbo', false);
setHTMLElemStyle('display', 'none', 'Anno_WRG', false);
setHTMLElemStyle('display', 'none', 'Anno_Capman', false);
setHTMLElemStyle('display', 'none', 'Anno_DV_Valve', false);

handleAnnot(true, 'i', 'Anno_GasCabinet', '', 'Anno_GasCabinet', undefined);
handleAnnot(true, 'i', 'Anno_MFC', '', 'Anno_MFC', undefined);
handleAnnot(true, 'i', 'Anno_PB_Valve', '', 'Anno_PB_Valve', undefined);
setHTMLElemStyle('display', 'none', 'Anno_GasCabinet', false);
setHTMLElemStyle('display', 'none', 'Anno_MFC', false);
setHTMLElemStyle('display', 'none', 'Anno_PB_Valve', false);

handleAnnot(true, 'i', 'Anno_Vac', '', 'Anno_Vac', undefined);
setHTMLElemStyle('display', 'none', 'Anno_Vac', false);

eventHTMLElem('click', 'Anno_Vac', false, function(event) {
    getJSFunction('changeIframe')('https://en.wikipedia.org/wiki/Vacuum_chamber');
});

eventHTMLElem('click', 'Anno_GasCabinet', false, function(event) {
    getJSFunction('changeIframe')('https://en.wikipedia.org/wiki/Gas_cabinet');
});
eventHTMLElem('click', 'Anno_MFC', false, function(event) {
    getJSFunction('changeIframe')('https://en.wikipedia.org/wiki/Mass_flow_controller');
});
eventHTMLElem('click', 'Anno_PB_Valve', false, function(event) {
    getJSFunction('changeIframe')('https://en.wikipedia.org/wiki/Diaphragm_valve');
});

eventHTMLElem('click', 'Anno_Throttle', false, function(event) {
    getJSFunction('changeIframe')('https://en.wikipedia.org/wiki/Throttle');
});
eventHTMLElem('click', 'Anno_Turbo', false, function(event) {
    getJSFunction('changeIframe')('https://en.wikipedia.org/wiki/Turbomolecular_pump');
});
eventHTMLElem('click', 'Anno_WRG', false, function(event) {
    getJSFunction('changeIframe')('https://www.teledyne-hi.com/en-us/what-we-do/wide-range-vacuum-gauge');
});
eventHTMLElem('click', 'Anno_Capman', false, function(event) {
    getJSFunction('changeIframe')('https://www.mks.com/n/capacitance-manometers');
});
eventHTMLElem('click', 'Anno_DV_Valve', false, function(event) {
    getJSFunction('changeIframe')('https://en.wikipedia.org/wiki/Diaphragm_valve');
});

eventHTMLElem('click', 'return-btn', true, function(event) {
    if (frontCabOpen) {
        UnAnim1();
    }
    if (backCabOpen) {
        UnAnim2();
    }
    if (vacChambOpen) {
        UnAnim3();
    }
});

eventHTMLElem('click', 'Anim_FrontCab', false, function(event) {
    frontCabOpen = true;
    setActiveCamera('Camera_FrontCab');
    operateAnimation('STOP', 'Door_FrontL', null, null, 'AUTO', 1, function() {}, false);
    operateAnimation('STOP', 'Door_FrontR', null, null, 'AUTO', 1, function() {}, false);
    operateAnimation('PLAY', 'Door_FrontL', 1, 60, 'LoopOnce', 1, function() {}, false);
    operateAnimation('PLAY', 'Door_FrontR', 1, 60, 'LoopOnce', 1, function() {}, false);
    applyObjLocalTransform('Open', 'position', [100, 0, 0]);
    setCSSRuleStyle('display', 'flex', '.top-left-btn', true, '');
    setHTMLElemStyle('display', 'block', 'Anno_Throttle', false);
    setHTMLElemStyle('display', 'block', 'Anno_Turbo', false);
    setHTMLElemStyle('display', 'block', 'Anno_WRG', false);
    setHTMLElemStyle('display', 'block', 'Anno_Capman', false);
    setHTMLElemStyle('display', 'block', 'Anno_DV_Valve', false);
});

eventHTMLElem('click', 'Anim_BackCab', false, function(event) {
    backCabOpen = true;
    setActiveCamera('Camera_BackCab');
    operateAnimation('STOP', 'Door_Back', null, null, 'AUTO', 1, function() {}, false);
    operateAnimation('PLAY', 'Door_Back', 1, 60, 'LoopOnce', 1, function() {}, false);
    applyObjLocalTransform('Open2', 'position', [100, 0, 0]);
    setCSSRuleStyle('display', 'flex', '.top-left-btn', true, '');
    setHTMLElemStyle('display', 'block', 'Anno_GasCabinet', false);
    setHTMLElemStyle('display', 'block', 'Anno_MFC', false);
    setHTMLElemStyle('display', 'block', 'Anno_PB_Valve', false);
});

eventHTMLElem('click', 'Anim_Vac', false, function(event) {
    vacChambOpen = true;
    setActiveCamera('Camera_Vac');
    operateAnimation('STOP', 'Vacuum_Chamber', null, null, 'AUTO', 1, function() {}, false);
    operateAnimation('PLAY', 'Vacuum_Chamber', 1, 60, 'LoopOnce', 1, function() {}, false);
    applyObjLocalTransform('Open3', 'position', [100, 0, 0]);
    setCSSRuleStyle('display', 'flex', '.top-left-btn', true, '');
    setHTMLElemStyle('display', 'block', 'Anno_Vac', false);
});



} // end of PL.init function

PL.disposeListeners = function() {
    if (_pGlob) {
        _pGlob.eventListeners.forEach(({ target, type, listener, optionsOrUseCapture }) => {
            target.removeEventListener(type, listener, optionsOrUseCapture);
        });
        _pGlob.eventListeners.length = 0;
    }
}

PL.disposeHTMLElements = function() {
    if (_pGlob) {
        _pGlob.htmlElements.forEach(elem => {
            elem.remove();
        });
        _pGlob.htmlElements.clear();
    }
}

PL.disposeMaterialsCache = function() {
    if (_pGlob) {
        for (const mat of _pGlob.materialsCache.values()) {
            mat.dispose();
        }
        _pGlob.materialsCache.clear();
    }
}

PL.dispose = function() {
    PL.disposeListeners();
    PL.disposeHTMLElements();
    PL.disposeMaterialsCache();
    _pGlob = null;
    // backward compatibility
    if (v3d[Symbol.toStringTag] !== 'Module') {
        delete v3d.PL;
        delete v3d.puzzles;
    }
}



return PL;

}

export { createPL };
